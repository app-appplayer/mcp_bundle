name: mcp_bundle
description: Bundle schema, loader, validator, expression language, and port contracts providing the foundation for the MCP ecosystem.
version: 0.2.0
homepage: https://app-appplayer.github.io/makemind
repository: https://github.com/app-appplayer/mcp_bundle
issue_tracker: https://github.com/app-appplayer/mcp_bundle/issues
documentation: https://github.com/app-appplayer/mcp_bundle/wiki

topics:
  - bundle
  - mcp
  - ai
  - dart
  - ports

environment:
  sdk: ^3.0.0

dependencies:
  collection: ^1.18.0
  crypto: ^3.0.0
  json_annotation: ^4.8.1
  meta: ^1.9.0

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  lints: ^3.0.0
  mockito: ^5.4.0
  test: ^1.24.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

funding:
  - https://www.patreon.com/mcpdevstudio
