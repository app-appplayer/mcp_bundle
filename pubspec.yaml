name: mcp_bundle
description: Bundle schema, loader, validator, and expression language providing the foundation for the MCP knowledge ecosystem.
version: 0.1.0
homepage: https://app-appplayer.github.io/makemind
repository: https://github.com/app-appplayer/mcp_bundle
issue_tracker: https://github.com/app-appplayer/mcp_bundle/issues
documentation: https://github.com/app-appplayer/mcp_bundle/wiki

topics:
  - bundle
  - mcp
  - ai
  - dart
  - schema

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.9.0
  collection: ^1.18.0
  json_annotation: ^4.8.1
  crypto: ^3.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
  mockito: ^5.4.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

funding:
  - https://www.patreon.com/mcpdevstudio
